class Solution(object):
    def isAnagram(self, s, t):
        if len(s) != len(t):
            return False
        contents = set()
        for x in s:
            if x not in contents:
                contents.add(x)
                if s.count(x) != t.count(x):
                    return False
        return True
